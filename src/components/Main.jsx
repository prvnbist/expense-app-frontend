import React, {Component} from 'react';

export default class Main extends Component {
    render() {
        return (
            <main>
                <div id="section-title">
                    <h1>EXPENSES</h1>
                    <button>+</button>
                </div>
            </main>
        )
    }
}
